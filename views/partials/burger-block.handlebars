<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devoured}}Yum that was dellicious!{{else}}So ready to devour that burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>