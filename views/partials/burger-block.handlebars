
	{{burger}}

	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devoured}}DEVOUR{{else}}Order Another{{/if}}
	</button>

	<button class="delete-burger" type="submit" data-id="{{this.id}}">DELETE!</button>
